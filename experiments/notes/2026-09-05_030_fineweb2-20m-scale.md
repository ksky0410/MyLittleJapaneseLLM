# 実験030：FineWeb混合コーパスでの20M級モデル化

## 開始前の計画

実施日時は2026-09-05、担当者はCodexです。実験028では、約5M TokenのFineWeb混合コーパスを使った5M級モデルが500 stepでは十分に学習できず、実験029では同じ条件でstep数を2,500へ延長しています。実験029の結果を基準として、次にモデル容量だけを増やしたときにvalidation loss、生成文、Issue #1の固定会話評価がどの程度変わるかを確認します。

今回の仮説は、同じデータ・Tokenizer・学習stepで5M級から約20M級へ拡張すると、一般日本語validation lossが下がり、文法崩壊や短い繰り返しが減り、会話testの内容対応も改善するというものです。ただし、学習Token数は約5Mであり、モデルを大きくしてもデータが少なすぎれば過学習または汎化不足になる可能性があります。したがって、lossが下がっても会話能力の向上とは分けて解釈します。

今回はモデル容量以外を変えません。RoPE、RMSNorm、GQA、SwiGLU、KV cache、半精度、学習率変更、データ比率変更は別実験へ分離します。5M級の実験029が2,500 stepまで完了した後、同じFineWeb混合Token列を使って20Mモデルをランダム初期値から学習します。5Mと20Mは同じseed 42で初期化しますが、形状が異なるためcheckpointの重み移植は行いません。

## データとTokenizer

学習Token列は`artifacts/tokens/mixed-ja-token-budget-fineweb2-5m-v1-train.bin`で、4,999,958 Token、SHA-256 `54eb3fab617c94bda59899db4f78e6ac65665606219414a710e40cc8ccb8603c`です。source混合の実測Token比率と入力hashは`artifacts/corpus/mixed-ja-token-budget-fineweb2-5m-v1.manifest.json`にあります。FineWeb test由来validationは`artifacts/tokens/fineweb2-edu-japanese-v1-test.bin`で、2,061,459 Token、SHA-256 `36d8d5c8bc92de1e168b8c3de9dd4ee975dec66f6b644b83bfbf9b239877161c`です。

Tokenizerは`artifacts/tokenizer/mixed-ja-80-10-10-v2-unigram.model`を再利用し、SHA-256 `5bde054fb91da54cbf56673a6d25b630399d95ec331049e5fa2af1a8d60731e4`、vocab size 4,096を固定します。一般・会話・医療のvalidation Token列も実験029と同じものを使い、FineWeb testだけを追加domainとして評価します。Issue #1の会話評価は`experiments/evaluation/chat-test-v1.json`の固定manifestを使い、48例を完全に同じ条件で比較します。

## モデルと学習条件

20M級条件は`configs/fineweb2-mixed-ja-20m-2p5k.toml`に保存します。vocab size 4,096、dim 384、10層、6 heads、context length 256、MLP倍率4、absolute position embeddingで、`scripts/inspect_model.py`の概算は19,382,016 parametersです。実装上のbiasを含む実パラメータ数とは少し差があるため、最終的なmodel signatureと概算値を両方記録します。

batch size 8、最大2,500 step、evaluation interval 100、sample interval 100、evaluation batches 20、AdamW、learning rate 3e-4、minimum learning rate 3e-5、warmup 300、weight decay 0.1、seed 42を使います。5M級実験029と同じスケジュールで、変更は`dim`と`layers`だけです。出力先は`artifacts/checkpoints/fineweb2-mixed-ja-20m-2p5k/`と`artifacts/samples/fineweb2-mixed-ja-20m-2p5k/`へ分け、既存checkpointを上書きしません。

M3 MacBook Air 16GBでは、重みだけでなくAdamWの状態とforward/backward一時領域が必要です。開始前に50〜100 stepのsmokeを行い、checkpoint、step時間、RSSまたは取得可能なメモリ情報、Metalエラーの有無を確認します。smokeが正常なら2,500 step本学習へ進み、危険なメモリ増加やNaNが出た場合は停止して失敗として記録します。batch sizeやcontext lengthを下げる場合は、当初条件から変えた理由と別条件をノートへ追記します。

## 実行前の再現情報

実験029の2,500 step完了commitは`a88b262`です。形状確認ではvocab size 4,096、dim 384、10層、6 heads、context length 256、概算19,382,016 parametersとなりました。20M smoke用configのSHA-256は`077a38f19d2f8162b22a0282249c2e1302581e631822ba1cb702c3fdc70a2a56`、本学習configは`e713cc654a2fabef94412baaa592c35ceaa6197feaaa63ad6f388bb6e601cc83`です。Tokenizer、学習Token列、評価用selection manifestのhashは前節の値を固定し、実験029と同一にしました。

現在の対話セッション自体はMetalデバイスのない環境ですが、保存された20M成果物にはstep 1から2,500までのmetricsとcheckpoint metadataが揃っており、MLXが利用できる実行環境で本学習が完走したことを確認できます。現セッションからの再ロード評価はMetal初期化前に停止するため、既存の評価JSONを上書きしません。

## 成功・失敗の判断

50〜100 step smokeと2,500 step本学習がNaN、shape error、データ長エラー、Metal初期化エラーなしに完走し、100 stepごとのmetrics・checkpoint metadata・固定prompt生成文が保存されれば実行面は成功とします。一般validation lossまたはFineWeb validation lossが5M級step scaling基準から0.10以上低下することを性能面の有望な目安とします。

固定chat-test-v1ではEOS、平均生成長、Token overlapを保存し、人手レビューtemplateの3項目を自動で埋めません。train本文完全一致候補7例、履歴context超過33例、Token overlapの意味評価限界を明記し、loss改善だけで会話能力を認定しません。

## 実行コマンド

実験029の完了確認後、まず形状確認とsmokeを実施します。

```bash
.venv/bin/python scripts/inspect_model.py --config configs/fineweb2-mixed-ja-20m-2p5k.toml
.venv/bin/python scripts/train.py --config configs/fineweb2-mixed-ja-20m-smoke.toml
```

smokeは`configs/fineweb2-mixed-ja-20m-smoke.toml`で専用checkpointディレクトリへ出すため、本学習出力と混ざりません。smokeが成功した後に次を実行します。

```bash
.venv/bin/python scripts/train.py --config configs/fineweb2-mixed-ja-20m-2p5k.toml
```

## 実験中の記録

smoke条件は`configs/fineweb2-mixed-ja-20m-smoke.toml`です。step 1のtrain lossは8.774733、general validation lossは8.603182、step 100のtrain lossは6.472222、general validation lossは6.980376でした。100 stepのsummary上の経過時間は31.07秒で、NaNやshape errorはありませんでした。smokeのcheckpointとmetricsは`artifacts/checkpoints/fineweb2-mixed-ja-20m-smoke/`へ保存しました。

smoke成功後、本学習を`configs/fineweb2-mixed-ja-20m-2p5k.toml`で実行し、100 stepごとにmetrics・checkpoint metadata・固定prompt生成文を保存しました。step 500ではgeneral validation loss 5.905240、step 1,000では5.561660、step 2,000では5.194101、step 2,500では5.158014となり、学習は途中停止せず完走しました。step 2,500時点の累積経過時間は1,408.19秒で、summary上の全体時間は1,409.99秒です。

## 結果と解釈

2026-09-05、20M級モデルの2,500 step本学習を完走しました。最終train lossは3.837297、general validation lossは5.158014、perplexityは173.819、最終学習率は`0.0000300001`でした。最良checkpointは`artifacts/checkpoints/fineweb2-mixed-ja-20m-2p5k/step_002500.npz`で、summary上の最良validation lossも5.158014です。NaN、shape error、データ長エラー、途中停止は発生しませんでした。専用のRSS・温度計測は実施していないため、メモリと温度は未計測です。

実験029の5M・2,500 step条件と比較すると、general validation lossは5.290503から5.158014へ0.132489低下しました。会話validation lossは3.341280から3.248580へ0.092700低下し、医療validation lossは3.883457から3.637988へ0.245469低下しました。FineWeb test lossは4.560703から4.382245へ0.178458低下しました。事前に定めた「generalまたはFineWeb lossが0.10以上低下」という有望性の目安は満たしました。

固定chat-test-v1の48例では、EOS到達が48/48、平均生成長が6.67 Token、precision・recall・F1が0.1533、0.0659、0.0778でした。029のEOS 48/48、平均6.60 Token、F1 0.0723と比べ、F1は0.0055上がりましたが、生成長はほぼ変わりませんでした。層別F1はshort 0.0952、medium 0.0808、long 0.0572で、029のshort 0.0835、medium 0.0755、long 0.0579と比べると短・中履歴は改善し、長履歴はわずかに悪化しました。自動Token overlapだけから会話能力の向上を断定しないという実験方針は維持します。

固定promptの`今日は`に対するstep 2,500生成は、「今日は何は、そ、そりがよっそして」のように日本語らしい助詞や接続表現を含む一方、語順・文法・意味の崩れが多く残りました。step 100の短い出力「今日は ⁇ (ーととがのを」がstep 2,500で長い出力へ変化したため、モデル容量の増加によって学習できる分布は広がったものの、約5M Tokenという学習量では自然な文章を安定生成するには不足しています。生成サンプルはstep 0から2,500まで省略せず`artifacts/samples/fineweb2-mixed-ja-20m-2p5k/`へ保存しました。

今回の仮説は、validation lossについて支持されました。5Mモデルから20Mへ増やすだけで複数domainのlossが改善し、固定chat-testのoverlap F1も小幅に改善しました。しかし、改善幅は会話の意味品質に比べて小さく、長い履歴では改善しませんでした。現在のボトルネックは、モデル容量だけでなく、学習Token数、absolute position embedding、4,096語彙、会話データの量と形式にもあると考えます。特に5M Tokenで約19M parametersを学習しているため、次はモデルをさらに無制限に大きくするより、Wikipediaを追加したデータ量実験とsource別比較を先に行うのが妥当です。

人手レビューtemplateは作成済みですが、今回も人手評価そのものは未実施です。train本文完全一致候補7例、履歴context超過33例、Token overlapが短い相づちを過大評価しうる点を引き継ぎます。

成果物は、metricsとcheckpoint metadataを`artifacts/checkpoints/fineweb2-mixed-ja-20m-2p5k/`、固定promptと固定chatの生成文を`artifacts/samples/fineweb2-mixed-ja-20m-2p5k/`、domain評価を`artifacts/evaluations/fineweb2-mixed-ja-20m-2p5k-domains.json`、固定chat評価を`artifacts/evaluations/fineweb2-mixed-ja-20m-2p5k-test-chat-v1.json`へ保存します。checkpointの重み本体は大きいためGitへ追加せず、metadataとhashをこのノートへ残します。

metricsのSHA-256は`dcf6821c84b2c76a7a1159d99793ec0fd739fcbdd1852d9252472e5dc28300b`、summaryは`82cd190ee4b25ce2b97d82778494b34df4c58b4d68e6c000ce3b51ea81b3a9dd`、step 2,500 metadataは`7bb8aaa5f722adfccf6e27a14c2e7d36791704fdea33fd9869e48db5ff433c9d`、step 2,500 checkpointは`ff1740aaacf26a7357d8788a419f4700f1f1b7f1e40400d890beb8c027a87149`です。step 0生成文は`67d23cd5d92d4e227c3a9f29d56db788287d850c332bde7f7dee8ab9f5d003c5`、step 2,500生成文は`0511e9818e96956b1ee867edd4335f9ab2a30025ef081d4e718c694c1be3bb3e`、固定chat TXTは`715700398131e900e828ee93a6377a80f65a07bad5356777cf1e4ed620794bd7`です。domain評価JSONは`7210d55993f7f44bce5b9d99deb277a865373a36752f7ac2cbfbdf82ecd8de4a`、固定chat評価JSONは`b5de2cc720b5edea809bf25fc51a69536bca91c81cb446a621233fbacf6b70de`です。smokeのmetricsは`3ee71c60badbe9d0e884e84258484b3fde3bbaa153da1bd8f06013aba4710b2b`、summaryは`8a70890d68bbd239f0d1447a37f8d59a22a5bcbe411d1dd1fa680bf33ae15340`です。

## 次に試すこと

20M級で容量効果を確認した後、同じ20M・同じデータでRoPEへ置き換え、context length 512の影響を別実験として比較します。その後、SwiGLUまたはRMSNormを一つだけ導入し、現代的なdecoder-only構成の各要素を個別に検証します。
