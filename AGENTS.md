# AGENTS.md

## このリポジトリの目的

このリポジトリは、MacBook上で日本語の小型LLMを自作し、Tokenizer、Transformer、事前学習、評価、追加学習を実験するためのものです。

このプロジェクトでは、コードを書くことと同じくらい、実験の経過を記録することを重視します。後から結果を再現できない実験は、成功していても十分な成果とはみなしません。

## 最重要ルール：実験ノートを必ず残す

学習、評価、Tokenizer作成、データ処理、生成比較など、結果に影響する操作を行う場合は、実験ノートを更新してください。

「結果が良かった実験」だけでなく、次のような場合も必ず記録してください。

- 学習を始めたが、途中で停止した場合
- エラーやメモリ不足で失敗した場合
- 損失が下がらなかった場合
- 生成結果が悪化した場合
- データ、Tokenizer、モデル構造、ハイパーパラメータを変更した場合
- 予定していた実験を実施しなかった場合

実験を開始する前に予定を記録し、実験が終わった直後に結果と解釈を記録してください。作業を後から思い出してまとめるのではなく、作業中にこまめに追記してください。

## 実験を始める前に記録する内容

学習コマンドを実行する前に、`experiments/notes/` に実験ノートを作成または追記してください。少なくとも次の内容を記録します。

- 実験番号、実施日時、担当者
- 今回確かめたいこと
- 事前の予想、または比較したい仮説
- 使用するGitコミット、ブランチ、変更内容
- データセットの出所、前処理、使用トークン数
- Tokenizerの種類、語彙数、バージョン
- モデルのパラメータ数、層数、次元数、ヘッド数、コンテキスト長
- バッチサイズ、学習率、スケジュール、オプティマイザ、シード
- 使用するコマンド
- 期待する結果と、成功・失敗を判断する基準

設定をファイルにまとめている場合でも、ノートから設定ファイルの場所とバージョンが分かるようにしてください。

## 実験中に記録する内容

長時間の学習では、少なくとも一定間隔ごとに記録してください。間隔は実験の規模に応じて決めますが、原則として1,000 stepを超えて記録を空けないでください。短いデバッグ実験では、開始時・異常発生時・終了時に記録します。

次の情報を可能な範囲で残してください。

- step、epoch、経過時間
- train loss、validation loss、perplexity
- 学習率、処理速度、累積トークン数
- メモリ使用量や温度など、問題の原因になりそうな情報
- 固定プロンプトに対する生成結果
- 警告、エラー、途中停止、設定変更

生成結果は、lossの数値だけでは分からない変化を確認するために保存します。学習途中の出力を省略せず、悪い出力や崩れた出力も残してください。

## 実験終了後に必ず記録する内容

実験を終了したら、できるだけその場で次の内容を追記してください。

- 実際に実行した条件。予定から変わった点を含む
- 最終的なtrain loss、validation loss、perplexity
- 最良のcheckpointと、そのstep
- 学習時間、最大メモリ使用量、停止理由
- 固定プロンプトによる生成例へのリンクまたは本文
- 事前の予想と一致した点、外れた点
- 今回分かったこと
- 次に試す変更を一つか二つ

失敗した実験は削除したり、成功したように書き換えたりしないでください。失敗理由が分からない場合は「原因不明」と明記し、次に調べることを残してください。

## 実験ノートの保存場所と命名

実験ノートは `experiments/notes/` にMarkdown形式で保存します。原則として、ひとつの独立した実験につきひとつのファイルを作成します。

ファイル名は次の形式にしてください。

```text
YYYY-MM-DD_NNN_短い名前.md
```

例：

```text
2026-09-05_001_tokenizer-vocab-8000.md
2026-09-06_002_pretrain-5m-debug.md
2026-09-08_003_pretrain-20m-baseline.md
```

実験の途中で新しい仮説や大きな設定変更が生じた場合は、同じノートに追記するか、新しい実験番号を発行してください。過去の記録を上書きしてはいけません。

## 再現性のルール

実験結果を報告する際は、結果だけでなく、再実行に必要な情報も残してください。

- Gitコミットまたは変更内容を明記する
- 乱数シードを固定し、記録する
- 使用したデータの取得元と加工方法を記録する
- 学習コマンドをそのまま残す
- チェックポイント、生成サンプル、評価結果の保存場所を記録する
- 依存パッケージやPythonの環境に差がある場合は記録する

大きなデータセットやcheckpointをGitに追加する必要はありません。ただし、ファイルの保存場所、取得方法、ハッシュ値、作成日時などをノートに残してください。

## 作業完了の条件

コードの変更だけでは作業完了としません。実験を実施した場合は、対応する実験ノートが更新され、生成サンプルや評価結果への参照が残っていることを確認してください。

実験を実施していない場合は、そのことを作業記録に明記してください。曖昧な「確認済み」「問題なし」という表現だけで終わらせず、何を確認したのかを書いてください。

## 実験ノートの書き方

短いメモで済ませず、未来の自分が読んで実験の意味と結果を理解できる文章で書いてください。数値だけを並べるのではなく、「なぜこの設定にしたか」「結果から何が言えるか」「次に何を変えるか」まで記録します。

なお、実験ノートを書くために学習の流れを止めすぎる必要はありません。開始前に最低限の予定を書き、学習中は節目に追記し、終了直後に解釈を完成させる運用を基本とします。
